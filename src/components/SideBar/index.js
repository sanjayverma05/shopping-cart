import React from 'react';
import FilterSidebar from 'Components/FilterSidebar';

const SideBar = () => {
    return <div className="shopping-cart__sidebar">
        <FilterSidebar />
    </div>
};

export default SideBar;